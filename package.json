{
  "name": "retailpulse-fake-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --host 0.0.0.0 --port $PORT",
    "dev": "json-server --watch db.json --routes routes.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}